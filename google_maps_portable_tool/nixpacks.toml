[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "playwright install chromium"
]

[start]
cmd = "streamlit run lead_app.py --server.port=$PORT --server.address=0.0.0.0"